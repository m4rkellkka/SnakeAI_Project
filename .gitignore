__pycache__/
*.pyc
.DS_Store

# Local training checkpoints — not committed, only pretrained.pth
model/checkpoint_last.pth
model/checkpoint_best.pth

# Regenerated each training run (committed only in assets/)
/learning_curve.png

# Sweep run checkpoints/plots (model/<run-name>/...)
model/*/checkpoint_*.pth
model/*/learning_curve.png

# Benchmark dashboard temp plots
/temp_benchmark_*.png

.claude/
venv/
.venv/

# AI assistant guidance — kept locally only
CLAUDE.md
